import { MapExplorer } from "../map/MapExplorer";

export const metadata = {
  title: "Explore — ReelCaster",
};

// Canonical consumer map surface.
export default function ExplorePage() {
  return <MapExplorer />;
}
