/**
 * /m/explore renders inside the same wrapper /explore does, re-exported rather
 * than copied so the fonts and theme can never drift between the two routes.
 */
export { default } from "@/app/explore/layout";
