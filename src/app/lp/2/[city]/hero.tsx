/**
 * /lp/2 hero — copy-led. The markup moved to _shared/lp-copy-hero.tsx when
 * /lp/4 started rendering the same hero with targeted copy: two files drifting
 * apart is exactly what would make that comparison unreadable.
 *
 * The counterpart is /lp/3, which leads with a photo. Same copy, same CTA,
 * same body — the hero is the variable under test there.
 */
export { default } from "../../_shared/lp-copy-hero";
